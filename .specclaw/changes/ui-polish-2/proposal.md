# Proposal: UI Polish Round 2 — Approval Queue, Tenant Filtering, Post Creation

## Problem
Dashboard lacks an approval queue. Posts/dashboard don't filter by tenant. Post creation UX needs media upload and scheduling. MCP server has TS errors.

## Proposed Changes
1. Approval queue on dashboard — quick approve/reject buttons
2. Tenant-scoped filtering on posts page and dashboard
3. Post creation page polish — platform select, media upload, schedule date
4. Fix MCP server TypeScript errors
