# Proposal: Mobile Responsive UI

## Problem
The dashboard uses a fixed 256px sidebar and desktop-only grid layouts. On mobile devices the sidebar is always visible, content overflows horizontally, and buttons are too small for touch.

## Proposed Solution
Make the entire UI mobile-first with a collapsible sidebar overlay, responsive grids, and touch-friendly tap targets (44px min).
