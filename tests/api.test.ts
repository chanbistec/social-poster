import { describe, it, expect } from "vitest";

// Placeholder integration test
// Full tests will be added after the API stabilizes

describe("api smoke", () => {
  it("placeholder", () => {
    expect(true).toBe(true);
  });
});
