# Server secret for AES-256-GCM encryption (must be 32+ chars, hex-encoded 32 bytes recommended)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SERVER_SECRET=

# Path to SQLite database file
DATABASE_PATH=./data/social-poster.db
